# 株式会社YELLOW コーポレートサイト刷新 — 作業記録

最終更新: 2026-08-13
最新ファイル: `yellow_spacex_v8.html`（＋ `assets/video/` の操作動画12本）

---

## 1. 目的と方針

現行サイト（Swiss/Editorial のモノクロ、Canvas アニメーション主体）を、**SpaceX 型の大判ビジュアル構成**へ刷新する。

参考サイト: `https://www.spacex.com/humanspaceflight/overview`

前提として決めたこと。

- **日本語がメイン**。英語は右上のトグルで切り替える従属言語とする
- 写真・動画はダミーで進め、構造とタイポグラフィを先に固める
- 単一HTMLファイルを維持する（XServer へ1ファイル上げるだけで反映できる運用を崩さない）

---

## 2. バージョン履歴

| 版 | 内容 |
|---|---|
| v1 | SpaceX構造の初版。ヒーロー＋領域パネル5面＋実績＋会社情報＋採用＋お問い合わせ |
| v2 | ロゴを白に統一。ヘッダーに事業ドロップダウン（xBUILD / xAD / xINTERACTIVE）と3プロダクトのパネルを追加 |
| v3 | **A案**を採用。領域パネル5面を撤去し、各プロダクト直下の「内訳」帯へ畳み込み。全画面パネルが8面→3面に |
| v4 | ロゴを Bold 700 → Regular 400 へ。字間を .34em → .42em に拡大 |
| v4_flow | 参考として作成した連続メディア版（sticky＋クロスフェード）。**不採用** |
| v5 | v4 の構造に、3プロダクトの個別ページとハッシュルーターを追加 |
| v5_1 | v5 の調整版 |
| v5_2 | ヘッダー「事業」ドロップダウンから写真を撤去しインデックス型へ変更。あわせて個別ページのケイパビリティをTOPと同じ全画面パララックスパネルへ統一 |
| v6 | モノクロ処理を解除して**カラー**へ。全画面パネル15面すべてに仮の背景動画を敷き、スクロールで動画が連なる構成に。**動画は不採用** |
| v7_siteimg | v7 のダミー画像を**現行サイトの実制作物**に差し替えた試作 |
| v7 | v6 の動画を撤去し、**カラーの静止画**へ戻す。構造は v5_2 と同じ。ロゴを **Raleway Light 300** に変更。アクセス時のイントロを追加 |
| **v8** | **現行**。v7_siteimg をベースに、全画面パネル15面へ**実機デモを操作した録画**を敷いた動画版 |

---

## 3. 現在の構成

### ホーム（`#page-home`）

```
HERO                全画面 / 画像
xBUILD              全画面 / 画像
  └ 内訳            01 技術シーズの発掘 ／ 02 研究開発の支援
xAD                 全画面 / 画像
  └ 内訳            03 人流・行動データ分析 ／ 04 公的プログラムへの採択
xINTERACTIVE        全画面 / 画像
  └ 内訳            05 スポーツDX ／ 06 市場への展開
RECORD              実績4件のカードグリッド
COMPANY             左メディア／右情報の2分割
CAREERS             全画面 / 画像 ＋ 求人3件
CONTACT             全画面 / 画像 ＋ フォーム＋連絡先
FOOTER
```

**リズムの設計**: 大きなメディア面のあとに必ず静かな黒い帯が来るよう交互に配置している。パネルを連続させるより情報が入る。

### イントロ（アクセス時の演出）

サイトを開くと**黒背景の中央に大きなロゴ**（Raleway Light 300 / clamp(34px,6vw,76px) / 字間 .42em）が立ち上がり、約1.7秒後にオーバーレイが溶けてサイトが現れる。

```
0.18s   ロゴがフェードイン＋わずかに拡大（1.15s）
1.65s   オーバーレイが溶け始める（0.85s）。同時にヒーローの出現アニメーションを再生し直す
2.6s    オーバーレイを DOM から外す（display:none）
```

- 溶け始めと同時に `.rv` を再初期化するので、幕が引かれた瞬間にヒーローのテキストが立ち上がる
- イントロ中はスクロールをロック（`body.intro-lock`）
- `prefers-reduced-motion: reduce` ではイントロ自体を表示しない
- 毎回のアクセスで表示（頻度を絞る場合は sessionStorage でスキップ判定を足す）

### ヘッダー「事業」ドロップダウン

写真を3つ並べて選ばせる形はやめた。サムネイルが縦を食って、肝心の内訳と実績が読めないため。

現在は**写真ゼロのインデックス型**。全幅の帯に、左のリード文と3列の本文を置く。

```
BUSINESS                01                  02                  03
リード文                 xBUILD              xAD                 xINTERACTIVE
事業の一覧へ →           つくる。             届ける。             動かす。
                        一行説明             一行説明             一行説明
                        ─────────           ─────────           ─────────
                        01 技術シーズの発掘   03 人流・行動データ分析  05 スポーツDX
                        02 研究開発の支援     04 公的プログラムへの採択 06 市場への展開
                        ─────────           ─────────           ─────────
                        関連実績              関連実績             関連実績
                        TOLIC 加盟／BtoR      SOIP × いわきFC      WELLNEXUS 鎌倉 2026
```

- 内訳6件はすべてリンク。クリックで該当プロダクトの個別ページへ飛ぶ
- 「関連実績」は `#record` へのリンク。ナビの時点で実際の案件名が読める
- 3列の行位置（内訳の開始・関連実績）は `grid-template-rows: subgrid` で揃えている。非対応ブラウザでは列として崩れずに並ぶだけ
- 高さは約 360px。開いてもヒーローの見出しを潰さない
- 幅 1250px 以下では左のリード文を畳み、`BUSINESS` と `事業の一覧へ →` の1行に置き換える
- 文言は既存の辞書キーを流用（`x*_short` / `x*_title` / `s1_t`〜`s6_t` / `p*_sp4_k` / `p*_sp4_v`）。新規追加は `drop_eyebrow` / `drop_note` / `drop_all` の3キーのみ

### プロダクト個別ページ（`#page-xbuild` / `#page-xad` / `#page-xinteractive`）

SpaceX の Falcon 9・Dragon のページと同じ組み立て。

```
1. 全画面ヒーロー      プロダクト名（特大）＋タグライン＋ホームへ戻るリンク
2. Overview           大きめ本文でプロダクトを定義
3. ケイパビリティ 01    全画面パネル（TOPと同じ挙動）
4. ケイパビリティ 02    全画面パネル（TOPと同じ挙動）
5. Specifications      対象／提供形態／標準期間／関連実績の4行表
6. 次のプロダクト        xBUILD → xAD → xINTERACTIVE → xBUILD の巡回
```

**ケイパビリティの画像挙動（v5_2で変更）** — もとは `.split` の左右2分割（画像は半分・固定なし）で、TOPの大判パネルとは別物の見え方になっていた。現在は**TOPのプロダクトパネルと同じ `.panel` コンポーネントをそのまま使用**している。

- `background-attachment: fixed` の全画面パララックス。スクロールすると画像が抜けていく
- 下から上へのグラデーションスクリム、テキストは常に左下
- 構成も TOP と同一：アイブロウ（`Capability 01 — xAD`）＋見出し＋本文＋枠線ボタン、右端に `01 / 02` のインデックス
- 画像は横位置（2400×1350）へ差し替え。ID は据え置き（xBUILD 153/490、xAD 88/290、xINTERACTIVE 396/100）
- 900px 以下では TOP と同じく `background-attachment: scroll` に落ちる
- 追加した辞書キーは `cap_label`（日英とも "Capability"）の1件のみ
- `.split` は会社情報セクションで引き続き使用。`.split--rev` は未使用になったが、CSSは残してある

---

## 4. デザイン仕様

### 書体

| 用途 | 書体 |
|---|---|
| ロゴ | **Raleway Light 300** |
| ラテン（本文・UI） | **D-DIN**（SpaceX が実際に使用している書体） |
| 日本語 | **Noto Sans JP**（300 / 400 / 500 / 700 / 900） |

単一のフォントスタックで運用している。D-DIN に日本語グリフが無いため、文字単位で Noto Sans JP へ自動フォールバックする。

```css
--font: 'D-DIN','Noto Sans JP',-apple-system,BlinkMacSystemFont,'Helvetica Neue',Arial,sans-serif;
```

**日英のタイポグラフィ設計** — SpaceX のリズムは「大文字＋広い字間」が支えている。日本語では大文字化が使えないため、**アイブロウ（小見出し）だけを日英ともラテン大文字のまま残し**、見出し本体は EN のときだけ CSS で自動的に大文字化する。

```css
html[lang="en"] .title { text-transform: uppercase; }
```

### ロゴ

**Raleway Light 300**（v7 で採用）。現ロゴの決め手である **W の中央が字面の上まで伸びて左右の線と交差する形**を持つ書体で、O が真円に近い点、線の太さも一致する。

| | ヘッダー | フッター |
|---|---|---|
| サイズ | 22px | 20px |
| ウェイト | 300 | 300 |
| 字間 | .42em | .42em |

**サイズの根拠** — Raleway は D-DIN より大文字が小さい（同じ px でキャップハイト 15px 対 16px）。従来の D-DIN 21px と**同じキャップハイト16pxになる22px**に合わせている。フッターも同様に 19px → 20px。字面の幅は Raleway のほうが広いが、最も狭いデスクトップ幅（940px）でもナビとの間隔は左右88px、375px でもバーガーとの間隔は51pxあり、詰まらない。

ロゴだけ別スタックにしてある。本文・UI は D-DIN のまま。

```css
--font-logo: 'Raleway','D-DIN','Noto Sans JP',Arial,sans-serif;
```

検討した他の候補（いずれも W が交差する）: Montserrat ExtraLight 200（やや幅広）／ Josefin Sans Light 300（字面が小ぶり）／ Poiret One（細すぎて21pxでは線が飛ぶ）。

### 色

```css
--black:  #000;
--white:  #fff;
--grey:   #9b9b9b;
--grey-d: #5a5a5a;
--line:   rgba(255,255,255,.22);
--line-s: rgba(255,255,255,.10);
--yellow: #FFD93D;   /* ヒーローのスクロールインジケータ等、ごく一部のみ */
```

### 主要トークン

```css
--pad: 6.5vw;                    /* 左右の基準余白 */
--pad-b: clamp(56px,10vh,120px); /* パネル下部余白 */
--hdr-h: 76px;
--ls-ui: .22em;                  /* ナビ・ボタンのトラッキング */
--ls-eyebrow: .30em;
--ease: cubic-bezier(.22,1,.36,1);
```

### SpaceX から再現した要素

- 100vh のメディアパネル、テキストは常に左下
- `background-attachment: fixed` によるパララックス（スクロール時に画像が抜けていく）
- 下から上へのグラデーションスクリム＋左からの補助スクリム
- 枠線ボタン（ホバーで白背景に反転）
- スクロールで黒く沈むヘッダー、全画面メニュー、現在地のナビ下線
- Specifications 表（ヘアラインで区切った定義リスト）

---

## 5. 実装仕様

### i18n

- `data-i18n` 属性でキーを指定。HTML を含む値は `data-i18n-html="true"`
- 辞書は **162キー、日英対称**（検証済み）
- 既定は `ja`。`localStorage` の `yellow_lang` に保存
- 切替時に `<html lang>`、`document.title`、`meta[name=description]` も更新

**ブランド名の小文字保護** — ナビ周りは全体が `text-transform: uppercase` のため、放置すると `xBUILD` が `XBUILD` になる。`.nt { text-transform: none; }` をブランド名にだけ当てている。辞書側の値にも `<span class="nt">` を含めている。

### ルーター

```js
var PAGES = ['home', 'xbuild', 'xad', 'xinteractive'];
```

- URL 形式: `#/xbuild` `#/xad` `#/xinteractive`
- `history.pushState` を使用。ブラウザの戻る／進むが機能し、リンクの直接共有も可能
- ハッシュが `#/` 以外（例 `#record`）の場合はホームへ切り替えてからその位置までスクロール
- ページ切替時に `.rv`（出現アニメーション）を初期化して再生

### ダミー画像

`picsum.photos` の**固定ID**を使用（seed ではなくID指定なので毎回同じ画像が出る）。

| 用途 | ID | 内容 |
|---|---|---|
| ヒーロー | 84 | 吊橋の構造体 |
| xBUILD | 315 | 見上げた建築の躯体 |
| xAD | 441 | 街路を行き交う人 |
| xINTERACTIVE | 452 | 手を挙げる観客 |
| ケイパビリティ他 | 88 / 100 / 153 / 190 / 268 / 290 / 396 / 417 / 437 / 478 / 490 | — |

ヘッダーのドロップダウンは v5_2 で写真を撤去したため、画像は使っていない。

**v6 でカラーへ切り替え、v7 でもそのまま。** グレースケール処理は CSS に残してあるが既定では効いていない。`<body>` に `ph` を付け直せば写真がまとめてモノクロに戻る。

```css
body.ph .panel__media,
body.ph .split__media,
body.ph .card__img img { filter: grayscale(1) contrast(1.08) brightness(.94); }
```

### スマホ対応

実機プロファイル（iPhone 13 / iPhone SE / Pixel 5 / iPad Mini）× 全6テンプレートで実測して調整した。

**直したこと**

- **iOSの自動ズーム** — 入力欄が15pxだったため、フォーカス時にiOSが画面を勝手に拡大していた。16pxへ（`.field input/select/textarea`）
- **タップ領域を44px以上に** — 実測で31要素が44px未満だった。バーガーは見た目を変えずに疑似要素（`::after`）で当たり判定だけ44×44へ拡張。言語切替・ロゴ・メニュー項目（48px）・フッター・事例のリンク・「戻る」・デモボタン・入力欄にも最小高を指定
- **画像上の可読性** — スマホは本文が画像の明るい部分に重なりやすいため、`.panel__scrim` のグラデーションを強めた
- **全画面メニュー** — 行間を広げ、項目を48pxにして誤タップを防止
- **横向き** — ランドスケープでは全画面パネルの `min-height` を解除し、内容が縦に潰れないようにした

**検証結果** — 4端末 × 6ページすべてで、横スクロールなし・44px未満の操作要素なし・JSエラーなし。

**実装上の注意** — このCSSは**スタイルシートの最後**に置く必要がある。前方に入れると後続の宣言（`.field input` など）に上書きされ、無効になる（実際に一度その状態になった）。

### 事例（/cases/）— 今後はYELLOW側で追加

旧サイトの17件を `cases.json` に移植済み。**追加・修正は `cases.json` の編集＋`node build-static.js` だけ**で、HTMLは触らない。手順は `CASES_HOWTO.md`、雛形は `cases.template.json`。

- 1件追加すると、詳細ページ・一覧カード・`sitemap.xml`・`llms.txt`・JSON-LD がすべて自動更新される
- ビルド時に検証が走り、必須項目の欠落・`key`の重複や不正・画像の不在・`demo`のURL形式を**日本語で指摘して停止**する
- `key` がそのままURL（`/cases/<key>/`）になるため、公開後の変更は既存リンクが切れる点に注意
- `extract-cases.js` は旧サイトからの一回限りの移行用。通常運用では使わない（`cases.json` を上書きするため）

**スクリプトの可搬性** — `build-static.js` / `cases-append.js` は `__dirname` 基準で動くので、リポジトリごと移動しても、どのディレクトリから実行しても動く。

### SEO・AI検索対応（静的マルチページビルド）

**背景** — v8 までは6ページ分の内容が1つのURLに同居していた（`#/xbuild` などのハッシュルーター）。検索エンジンは `#` 以降を別URLと見なさないため、**実質1ページしかインデックスされない**。AI検索も引用できるURLがトップだけになる。

`build-static.js` が v8 から `dist/` を生成する。**ドメインを変える場合は先頭の `ORIGIN` 1行だけ**。

```
dist/
  index.html            /                105KB
  xbuild/index.html     /xbuild/          92KB
  xad/index.html        /xad/             92KB
  xinteractive/index.html                 92KB
  privacy/ terms/                     88KB / 87KB
  assets/img/*.webp|jpg  17枚（全ページ共有）
  assets/video/*.mp4     12本
  robots.txt  sitemap.xml  llms.txt  og.png  404.html
  .htaccess（XServer）  _headers（Netlify）
```

**各ページに持たせたもの**

- 独自の title / description / canonical / OGP、`<h1>` は1ページ1本
- JSON-LD を `@graph` 化。Organization ＋ WebSite ＋ WebPage を全ページに、プロダクトページには Service ＋ OfferCatalog ＋ BreadcrumbList を追加
- `setLang` がページ別 meta を尊重（辞書にページ別 meta を日英対称で追加）

**軽量化** — 埋め込みbase64画像17枚を `/assets/img/` へ外部化し、**1ページ 5.44MB → 105KB（52分の1）**。全ページで共有されるためブラウザキャッシュも効く。

**AI検索対応**

- `llms.txt` を新設。会社概要・3プロダクト・実績・会社情報を、AIが読む前提の構造で要約
- `robots.txt` で GPTBot / OAI-SearchBot / ClaudeBot / PerplexityBot / Google-Extended / Applebot-Extended / CCBot を明示的に許可
- 主要なAIクローラはJSを実行しないため、**JS非実行でも本文が読めること**を実測（トップ2771字、各プロダクト約1050字）

**検証済み** — 全URLが200／`<h1>`各1本／4xxなし／JSエラーなし／ページ間遷移とセクションアンカー／EN切替でもページ別titleを維持。

**ドメイン確定** — `https://yellow.technology` 直下への置き換えで確定。現行サイトはHTML1枚のみのため、パス単位の301リダイレクトは不要。旧アンカー（`#case-01`〜17 / `#about` / `#profile` / `#intro`）はJSで新URLへ転送している。

### 現行サイトの画像をはめた試作（`yellow_spacex_v7_siteimg.html`）

picsum のダミー20箇所（15種）を、現行サイトに載っている実制作物の画像に差し替えたもの。画像は webp のまま data URI で埋め込んであるので**単一HTMLのまま**（2.5MB）。

**選定の方法** — 現行サイトのHTMLから90点の画像を抜き出し、次の順で絞った。

1. **テキストが載る左下ゾーンの明るさを実測**。全画面パネルは白文字が載るため、明るい画像は使えない（運用型広告の解析ボードや店頭販促のフロアビューは明るさ246〜249で全滅）
2. **同じゾーンのエッジ量（ごちゃつき）を実測**。画像そのものに細かい文字が入っていると本文と競合する。分析ダッシュボード系はここで落ちる
3. 残った暗く静かな画像を、事業の系統（つくる／届ける／動かす）に寄せて割り当て

結果として、**点群データモデリング・駅構内3D・工場移転シミュレーション**といった黒地の3Dビューが中心になった。スポーツDXの枠にはスタジアムの点群がそのまま入っている。

**わかったこと**

- 黒地の3Dビュー・点群は、ダミー写真より明確に良い。SpaceXの大判構成と相性がよく、事業内容もそのまま伝わる
- 一方で**すべてアプリのスクリーンショットなので、画像自体のUI（スライダー、レイヤーの凡例、フロア切替）が背景に写り込む**。左サイドにUIがある画像は本文と重なるため避けた
- 明るいダッシュボード系（運用型広告・店頭販促）は内容としては一番強いが、全画面の背景には使えない。実績カードや専用セクションで使うほうが活きる
- 本番では**UIを外した書き出し**か、UI部分を除いたトリミングがあると理想的

### 実機デモの操作動画（v8）

現行サイトで公開されている**実際に動くWebアプリ**（Netlify 上のデモ）をローカルに取り込み、**ブラウザ上で本当に操作しながら録画**したもの。合成ではなく、経路検索や人流切替は実際にアプリが計算・描画している。

| クリップ | アプリ | 操作内容 |
|---|---|---|
| hero | 新宿駅 構内図 3D | 大きく周回 |
| xbuild | 新宿駅 構内図 3D | 積層ビューを近接周回 |
| xbuild-cap01 | 新宿駅 構内図 3D | **経路検索モード**で経路を引く（距離・徒歩分数が算出される） |
| xad-cap02 | 新宿駅 構内図 3D | 人流シミュレーション 朝→昼→夜 |
| contact | 新宿駅 構内図 3D | フロア展開 ×1→×3→×1 |
| xinteractive | 渋谷駅 構内図 3D | 周回＋人流の夜切替 |
| xbuild-cap02 | 渋谷駅 構内図 3D | **図面モード**（平面）に入り、戻る |
| xad-cap01 | 渋谷駅 構内図 3D | フロア展開の往復 |
| xad | 渋谷駅 構内図 3D | 歩行者ネットワークの周回 |
| xinteractive-cap01 | xVIEW 点群ビューア | スタジアム点群のシーケンス再生＋周回 |
| xinteractive-cap02 | xVIEW 点群ビューア | ピッチ低空フライオーバー |
| careers | xVIEW 点群ビューア | コンコース手持ち撮影シーン |

仕様: H.264 / **1280×720ネイティブ** / 25fps / 約8秒ループ / 音声なし / CRF23 / 合計約71MB。

**画質の改訂（同日）** — 当初は 1280×720 の撮影フレームを 1600×900 へ引き伸ばして CRF30 で圧縮していたが、①引き伸ばしをやめて撮影解像度のまま、②CRF23・preset slow に変更。引き伸ばし1世代分と圧縮ノイズが消え、点群の細線が明確に残る。拡大にはブラウザ側のスケーラを使う（cover表示のため見た目の構図は不変）。

**録画の方法** — ソフトウェアGPUでは実時間録画が0.4fpsしか出ないため、**仮想時間（CDP Emulation.setVirtualTimePolicy）を80msずつ進めて1フレームずつ確定的に撮影**し（12.5fps相当）、minterpolate で25fpsに補間した。入力イベントは仮想時間停止中は ack が返らないため、await せず投げてから時間を進める方式。操作は sin 往復で組んであり、終端が始端に近づくためループの継ぎ目が目立ちにくい。録画スクリプトは作業用スクラッチの `record3.js`（リポジトリ外）。

**パネルの背景静止画はクリップの先頭フレーム**に差し替えてあるので、動画が始まる瞬間の飛びがない。読み込み・再生・視差の仕組みは v6 と同じ（下記）。

### 単一ファイル版（Netlify 確認用）

`yellow_spacex_v8_single.html`（30.6MB）— v8 の動画を base64 で埋め込んだ完全単一ファイル。Netlify Drop に `index.html` 1枚で上げて確認する用途。

- 動画は CRF23 マスターから **CRF31 で1世代だけ**再圧縮（計19MB）。当初の「CRF30→CRF33 の二重圧縮」をやめたため、サイズ同等で画質は上
- xbuild / xad / xinteractive はホームと個別ページの2箇所で使われるため、素朴に埋め込むと二重になる。**`window.__VID` 辞書に1本1回だけ埋め込み**、パネル側は `data-src="vid:名前"` で参照する方式に変更（約7MB削減）
- ローダーは `vid:` プレフィックスを見て辞書から引く。分割ファイル版のパスもそのまま通る
- 30MBを一括ダウンロードしてから描画される。**確認用であり、本番は v8 ＋ assets/video の分割構成を推奨**
- git には含めない（.gitignore 済み）

**さらに画質を上げる場合の選択肢**

1. **分割のままNetlifyへ**（推奨・すぐ）: Netlify Drop はフォルダごと投げられる。`index.html`＋`assets/video/` の構成なら単一ファイルの容量制約が消え、CRF23 の71MB版をそのまま確認できる
2. **再録画**（時間がかかる・上限突破）: 現状の撮影自体が 1280×720 の JPEG・12.5fps補間。1920×1080・PNG・実25fps（仮想時間40ms刻み）で撮り直せば補間なしの実フレームになる。この環境では約4〜5時間
3. 本番は実GPUのあるマシンでの画面録画が最良（この環境の制約が全部消える）

### 仮の背景動画（v6・不採用）

**v7 で撤去。** 静止画で進めることになったため、以下は動画を再検討する場合の記録。実装済みの `yellow_spacex_v6.html` と `assets/video/` はそのまま残してある。

全画面パネル**15面すべて**に動画を敷き、スクロールすると動画が次々に現れる構成だった。

`assets/video/` に12本。**同じダミー写真からこちらで生成した仮素材**で、ゆっくりズーム＋パンさせただけのもの。始点と終点が一致するよう1周期の cos/sin で往復させてあるため、ループの継ぎ目が出ない。

| ファイル | 使用箇所 | 元画像ID |
|---|---|---|
| `hero.mp4` | ホームのヒーロー | 84 |
| `xbuild.mp4` | ホーム xBUILD ／ xBUILDページのヒーロー | 315 |
| `xad.mp4` | ホーム xAD ／ xADページのヒーロー | 441 |
| `xinteractive.mp4` | ホーム xINTERACTIVE ／ 同ページのヒーロー | 452 |
| `careers.mp4` / `contact.mp4` | 採用 ／ お問い合わせ | 29 / 417 |
| `xbuild-cap01.mp4` `xbuild-cap02.mp4` | xBUILDページの内訳 | 153 / 490 |
| `xad-cap01.mp4` `xad-cap02.mp4` | xADページの内訳 | 88 / 290 |
| `xinteractive-cap01.mp4` `xinteractive-cap02.mp4` | xINTERACTIVEページの内訳 | 396 / 100 |

仕様: H.264 / 1600×900 / 25fps / 12秒ループ / 音声なし。合計 約7.5MB。

**読み込みと再生**

- `preload="none"` ＋ `data-src`。IntersectionObserver で**画面に入る手前まで来た動画だけ**を読み込む。12本を一度に落とさない
- 画面から外れたら `pause()`。戻れば再開する
- ページ切替時は `showPage()` から `window.__vidSync()` を呼び、隠れたページの動画を止めて表示側を再生させる
- `poster` に元のダミー写真を指定してあるので、動画が無い／読めない環境でも静止画が出る。CSS背景もフォールバックとして残してある
- `prefers-reduced-motion: reduce` では読み込まず、poster のまま静止する

**パララックス** — 静止画では `background-attachment: fixed` で「画像が抜けていく」動きを出していたが、これは動画には効かない。代わりに動画を縦に118%で敷き、スクロール量の18%だけ逆送りしている（`translate3d`、rAFで更新）。見え方は静止画のときとほぼ同じ。900px以下では切っている。

**戻す場合** — `yellow_spacex_v6.html` をベースにする。本番素材は `assets/video/` の同名ファイルを置き換えるだけでよく、HTML側の変更は不要。

### お問い合わせフォーム

既存の `contact.php` にそのまま POST する。フィールド名は現行サイトと同一（`name` / `org` / `email` / `topic` / `message` / `website`（ハニーポット））。`lang` を追加送信している。

### スクロールスナップ

全画面パネルと帯が交互になるとスナップが引っ張り合うため、**既定でオフ**。有効化する場合は CSS の該当行のコメントを外す。

```css
/* html{scroll-snap-type:y proximity;} */
```

---

## 6. 引き継いだ制約

- **JS でテンプレートリテラル（バッククォート）を使用しない** — Python のファイル編集ツールと干渉するため。全て文字列連結で記述している
- i18n 辞書は日英を厳密に分離し、HTML のフォールバック文言と辞書の値を同期させる
- 成人向け業界に関する記述はサイト内容から除外する
- お問い合わせは mailto ハンドオフを使わず、必ずサーバサイドで処理する

---

## 7. 未決事項

### 優先度：高

**3プロダクトの実際の定義**
xBUILD / xAD / xINTERACTIVE の内容は名称からの推測で「つくる／届ける／動かす」と仮置きしている。とくに **xAD を広告事業と解釈した点**は未確認。実際の定義が分かれば、見出しコピー・本文・写真選定・内訳の割り当てをまとめて詰め直せる。

**Specifications の実データ**
「標準期間 6〜24ヶ月」等は根拠のない仮置き。SpaceX のスペック表が効いているのは数字が事実だからで、埋めるための数字はむしろ信頼を削る。実データを入れるか、行を「対象」「提供形態」だけに減らすかの判断が必要。

**「公的プログラムへの採択」の置き場所**
現在は xAD の内訳 04 に入れているが、SOIP も助成金も「届ける」事業ではなく、どのプロダクトにも共通してかかる会社の動き方であり、収まりが悪い。会社情報セクションへ「働きかた」として移すことを推奨。

### 2026-08-13 の作り込み（実施済み）

- **JS無効/失敗時の保険**: イントロに noscript＋5秒ウォッチドッグ。どう転んでもサイトが現れる
- **favicon / OGP / canonical / theme-color**: og.png(1200×630) 同梱。og:image と canonical のURLは本番ドメイン確定時に要確認
- **モバイルの動画抑制**: 900px以下と省データ設定では動画を読み込まず poster 静止画のまま
- **フォーム**: 未入力項目の下線・ラベルを赤に、aria-invalid 付与、最初の未入力欄へフォーカス。入力で即解除
- **キーボード操作**: スキップリンク／全画面メニューの focus trap（role=dialog、Esc でバーガーへ復帰）／--grey-d を #767676 へ（黒地4.5:1）
- **ヘッダー**: 下スクロールで隠れ、上スクロールで現れる。最上部・メニュー中・フォーカス中は常時表示
- **イントロ頻度**: セッション初回のみフル尺、2回目以降は短縮（sessionStorage）
- **SEOの残り**: Organization の JSON-LD を head へ。404.html / robots.txt / sitemap.xml を別ファイルで用意（URLは yellow.technology 仮置き。XServer で 404 を使う場合は .htaccess に `ErrorDocument 404 /404.html` が必要）
- **戻る/進むのスクロール位置復元**: 遷移時に現在位置を履歴へ記録し、popstate で瞬時復元（scrollRestoration=manual、スムーススクロールを一時停止して戻す）
- **ドロップダウンの残留解消**: 選択直後はカーソルが乗ったままでも閉じる。離れて戻れば従来どおりホバーで開く。キーボード操作では抑制を付けない（付けると解除機会が無いため）
- **法務ページ**: #/privacy #/terms をルーターページとして追加（**仮文面・要リーガル確認**・ja のみ）。フッターとメニューのリンクを接続

### 優先度：中

- **実績カードの飛び先**（未決）: 4枚とも #contact のまま。Journal を作るか実デモへ飛ばすか要判断
- **Specifications の実データ**（未決）: 標準期間などの仮数値をどうするか
- **求人の応募先**（未決): 応募する→ の先が無い
- **フォント自己ホスト**: D-DIN(野良CDN)・Raleway/Noto(Google Fonts) を woff2 同梱に切り替える
- **本番写真の調達**。現状はすべて picsum のダミーで、内容は事業と無関係。カラーで進めるため、写真同士のトーンが揃っているかは実素材の選定時に効いてくる
- ~~ロゴの書体~~ → **Raleway Light 300 で確定。v7 に反映済み**
- `no-reply@yellow.technology` を XServer メールパネルで作成（フォーム公開の前提条件）
- Journal（記事詳細）の扱い。実績カードは現在すべて `#contact` へのダミーリンク
- 現行サイトの資産（Lenis スムーススクロール、カスタムカーソル、GSAP 見出しアニメーション）を持ち込むか。SpaceX は意外と素っ気ない作りのため、足さない方が再現度は上がる
- プライバシーポリシー／利用規約ページ（現在はフッターからリンクのみ）

### 既知の挙動

- ドロップダウンからプロダクトを選んだあと、カーソルがドロップダウン上に残っているとメニューが開いたままになる（SpaceX も同挙動のため現状は放置）
- v5_2 で、Esc・外側クリックで閉じても `:focus-within` が残って開いたままになる不具合を修正（閉じる際に内側のフォーカスを外している）
- `fonts.cdnfonts.com` から D-DIN を読み込んでいる。本番では woff を XServer に置いて自己ホストすることを推奨
- Raleway（ロゴ）と Noto Sans JP は Google Fonts から1リクエストでまとめて読み込んでいる。こちらも本番では自己ホストが望ましい

---

## 8. ファイル一覧

| ファイル | 内容 |
|---|---|
| `yellow_spacex_v8.html` | **最新版**。実機デモの操作動画つき（assets/video が必要） |
| `yellow_spacex_v7.html` | カラーの静止画。単一HTMLで完結する |
| `yellow_spacex_v7_siteimg.html` | v7 の画像を現行サイトの実制作物に差し替えた試作（2.5MB） |
| `yellow_spacex_v6.html` | 全パネルに仮の背景動画（不採用・参考） |
| `assets/video/*.mp4` | v8 の操作動画12本（合計約29MB） |
| `yellow_spacex_v5_2.html` | ドロップダウンのインデックス型化＋個別ページのパネル統一 |
| `yellow_spacex_v5_1.html` | ドロップダウン変更前 |
| `yellow_spacex_v5.html` | ホーム＋プロダクト個別ページ3本 |
| `yellow_spacex_v4.html` | 個別ページ追加前 |
| `yellow_spacex_v4_flow.html` | 連続メディア版（不採用・参考） |
| `yellow_spacex_v3.html` | A案適用直後 |
| `yellow_spacex_v2.html` | プロダクト追加直後 |
| `yellow_spacex_v1.html` | 初版 |

# 事例（/cases/）の追加・修正手順

事例ページは `cases.json` 1ファイルから自動生成されます。
HTMLを直接さわる必要はありません。

---

## 手順

### 1. 画像を置く

`assets/case/` に画像を入れます。ファイル名は自由ですが、
他と重複しないよう `<事例のkey>-01.webp` のように揃えると管理しやすいです。

推奨: 横1200px以上、webp または jpg。1件あたり3〜6枚。

### 2. `cases.json` に追記

配列の末尾に、下のテンプレートをコピーして貼り付け、中身を書き換えます。
**一覧ページは `cases.json` の並び順で表示されます**（先頭が左上）。

```json
{
  "key": "sample-case",
  "tags": "製造",
  "eyebrow": "XBUILD ／ 3Dツイン／製造",
  "title": "事例のタイトル",
  "lead": "1〜3文の要約。一覧のカードと検索結果の説明文に使われます。",
  "demo": "https://example.netlify.app/",
  "imgs": [
    { "src": "/assets/case/sample-case-01.webp", "alt": "画面の説明" },
    { "src": "/assets/case/sample-case-02.webp", "alt": "画面の説明" }
  ],
  "sections": [
    { "heading": "案件背景", "paras": ["背景の説明。"], "items": [] },
    { "heading": "できること", "paras": [], "items": ["機能1", "機能2"] },
    { "heading": "活用シーン", "paras": ["想定される使い方。"], "items": [] }
  ],
  "spec": [
    ["対象", "工場敷地"],
    ["データソース", "敷地・建屋配置データ"],
    ["提供形態", "Webデモ（ブラウザ）"]
  ]
}
```

### 3. ビルド

```
node build-static.js
```

`dist/` が作り直されます。問題があれば**日本語で指摘して止まります**。

### 4. 公開

`dist/` の中身をそのままサーバへ上げます。

---

## 各項目の意味

| 項目 | 必須 | 説明 |
|---|---|---|
| `key` | ● | **URLになります**（`/cases/<key>/`）。英小文字・数字・ハイフンのみ。あとから変えると既存リンクが切れるので慎重に |
| `title` | ● | 事例名。ページの `<h1>` と `<title>` |
| `lead` | ● | 要約。カード・meta description・OGP・llms.txt に使われる。**検索結果に出る文章なので一番重要** |
| `eyebrow` | | 見出しの上の小さい文字。先頭を `XBUILD` / `XAD` / `XINTERACTIVE` にすると、その事業ページへのリンクが自動で付く |
| `demo` | | 「デモを開く」ボタンのURL。省略するとボタンが出ない |
| `imgs` | ● | 最低1枚。1枚目が一覧のサムネイル。詳細ページは最大6枚まで表示 |
| `sections` | | 本文。`paras` は段落の配列、`items` は箇条書きの配列。どちらか片方だけでも可 |
| `spec` | | 右側の仕様表。`["項目名", "値"]` の配列 |
| `tags` | | 分類メモ。`eyebrow` が事業名で始まらない場合、カードのラベルに使われる |

---

## 自動で行われること

事例を1件足すと、以下がすべて自動更新されます。

- `/cases/<key>/` のページ生成
- `/cases/` 一覧へのカード追加
- `sitemap.xml` へのURL追加
- `llms.txt` への1行追加（AI検索向け）
- 構造化データ（JSON-LD）の生成
  - 詳細ページ: `CreativeWork` + `BreadcrumbList`、`demo` があれば `workExample`
  - 一覧: `CollectionPage` + `ItemList`

## よくあるエラー

| メッセージ | 対処 |
|---|---|
| `「key」は英小文字・数字・ハイフンのみ` | 大文字・記号・日本語を使わない |
| `「key」が重複しています` | 他の事例と同じ `key` になっている |
| `画像が見つかりません` | `assets/case/` にファイルがあるか、`src` の綴りを確認。`src` は `/assets/case/...` から書く |
| `「demo」は http(s):// から始まるURLに` | `https://` を付ける |

## 注意

- `extract-cases.js` は**旧サイトからの一回限りの移行用**です。通常の追加では使いません（実行すると `cases.json` が上書きされます）
- ドメインを変える場合は `build-static.js` の先頭 `ORIGIN` の1行だけ変更してください

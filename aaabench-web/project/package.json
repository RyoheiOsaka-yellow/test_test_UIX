{
  "name": "agentcity",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The skeleton. An empty stage with working sensors — the world is the agent's job.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}


/* ===== v13: UIX ===== */
/* ヘッダー2段化: 階層クラムは1段目、表示/分析/ツールはツールバーへ */
#hdr{height:52px}
#toolbar{position:fixed;top:52px;left:0;right:0;height:40px;display:flex;align-items:center;justify-content:flex-end;gap:10px;
  padding:0 16px;z-index:29;pointer-events:none}
#toolbar>*{pointer-events:auto}
.tb-grp{display:flex;align-items:center;gap:5px;padding:4px 8px 4px 6px;border-radius:22px;
  background:rgba(21,24,33,.82);border:1px solid var(--line);backdrop-filter:blur(8px)}
.tb-grp .tb-l{font-size:9px;letter-spacing:.14em;color:var(--sub);padding:0 6px 0 4px;font-weight:700}
.tb-grp .crumb{padding:4px 10px;font-size:10.5px;white-space:nowrap}
#panel{top:100px;max-height:calc(100vh - 190px);width:304px;box-shadow:0 10px 30px rgba(0,0,0,.35)}
#panel-tab{top:100px}
#ticket-card,#bim-card,#vis-card{top:100px}
#vis-card{max-height:calc(100vh - 190px)}
#sv-exit{top:100px}
#sv-hud{top:142px}
#toast{top:102px}
#enter-hint{bottom:118px}

/* KPIタイル・セクションの質感 */
.kpi{background:linear-gradient(180deg,rgba(255,255,255,.035),rgba(255,255,255,0)) ,var(--panel2);transition:border-color .18s}
.kpi:hover{border-color:#3a4460}
.sec-t{cursor:pointer;user-select:none;display:flex;align-items:center;gap:6px}
.sec-t::after{content:'▾';margin-left:auto;color:var(--line);font-size:10px;transition:transform .2s}
.sec.fold .sec-t::after{transform:rotate(-90deg)}
.sec.fold>*:not(.sec-t){display:none}
.mode-btn,.chip,.tool-btn{transition:border-color .15s,background .15s,color .15s,transform .08s}
.mode-btn:active,.chip:active,.tool-btn:active{transform:scale(.985)}
.crumb:focus-visible,.chip:focus-visible,.mode-btn:focus-visible,button:focus-visible{outline:2px solid var(--brex);outline-offset:1px}

/* 視点プリセットメニュー */
#view-menu{position:fixed;top:96px;right:16px;z-index:46;display:none;min-width:236px;
  background:rgba(21,24,33,.97);border:1px solid var(--line);border-radius:12px;padding:8px;backdrop-filter:blur(10px);
  box-shadow:0 12px 32px rgba(0,0,0,.45)}
#view-menu .vm{display:flex;align-items:center;gap:9px;width:100%;padding:8px 10px;border:none;background:transparent;color:var(--txt);
  text-align:left;font-size:11.5px;border-radius:8px;cursor:pointer;font-family:var(--jp)}
#view-menu .vm:hover{background:rgba(245,196,0,.1);color:var(--brex)}
#view-menu .vm small{display:block;color:var(--sub);font-size:9.5px;margin-top:1px}
#view-menu .vm kbd{margin-left:auto;font-size:9px;color:var(--sub);border:1px solid var(--line);border-radius:4px;padding:0 5px}

/* ステータスバー */
#statusbar{position:fixed;left:12px;bottom:14px;z-index:24;display:flex;gap:0;align-items:center;
  background:rgba(21,24,33,.9);border:1px solid var(--line);border-radius:20px;padding:6px 6px 6px 12px;font-size:10.5px;color:var(--sub);backdrop-filter:blur(8px)}
#statusbar b{color:var(--txt);font-family:var(--mono);font-weight:500;letter-spacing:.04em}
#statusbar .sb{padding:0 10px;border-right:1px solid var(--line)}
#statusbar .sb:last-child{border-right:none}
#statusbar .fps{min-width:44px;text-align:right}

/* 起動スプラッシュ */
#splash{position:fixed;inset:0;z-index:100;background:#0d0f14;display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:14px;transition:opacity .6s ease;color:var(--txt)}
#splash.out{opacity:0;pointer-events:none}
#splash .lg{font-family:var(--mono);font-size:34px;font-weight:600;letter-spacing:.12em;color:#fff}
#splash .lg i{font-style:normal;color:var(--brex)}
#splash .tt{font-size:13px;color:var(--sub);letter-spacing:.1em}
#splash .bar{width:220px;height:3px;background:var(--line);border-radius:2px;overflow:hidden;margin-top:8px}
#splash .bar i{display:block;height:100%;width:40%;background:var(--brex);animation:sp-run 1.1s ease-in-out infinite}
@keyframes sp-run{0%{transform:translateX(-100%)}100%{transform:translateX(260%)}}
#splash .hint{font-size:10.5px;color:var(--sub);margin-top:10px}
#splash .hint kbd{border:1px solid var(--line);border-radius:4px;padding:0 5px;color:var(--txt);font-size:10px}

/* ヘルプ */
#help{position:fixed;inset:0;z-index:70;display:none;background:rgba(5,7,12,.78);backdrop-filter:blur(5px)}
#help .box{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:min(760px,92vw);max-height:86vh;overflow-y:auto;
  background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:22px 26px}
#help h3{font-size:14px;letter-spacing:.06em;border-left:3px solid var(--brex);padding-left:10px;margin-bottom:14px}
#help .cols{display:grid;grid-template-columns:1fr 1fr;gap:18px}
@media(max-width:760px){#help .cols{grid-template-columns:1fr}}
#help h4{font-size:10.5px;letter-spacing:.12em;color:var(--sub);margin:8px 0 6px}
#help .kr{display:flex;align-items:center;gap:8px;font-size:11.5px;padding:4px 0;border-bottom:1px dashed var(--line)}
#help .kr kbd{min-width:28px;text-align:center;border:1px solid var(--line);border-radius:5px;padding:1px 6px;font-size:10px;color:var(--brex);background:var(--panel2);font-family:var(--mono)}
#help .kr span{color:var(--txt)}
#help-x{position:absolute;top:14px;right:16px;padding:6px 14px;border-radius:8px;border:1px solid var(--line);background:var(--panel2);color:var(--txt);cursor:pointer;font-size:11px;font-family:var(--jp)}
#help-x:hover{border-color:var(--brex);color:var(--brex)}

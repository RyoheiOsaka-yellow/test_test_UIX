# Real Jev mode is enabled only when this key is present in the *server* environment
# (Vite dev server / proxy). The key is never shipped to the browser bundle.
TYPESAFE_API_KEY=
# Jev decision endpoint (structured decision API). Defaults to the placeholder below.
JEV_API_URL=https://api.typesafe.ai/v1/jev/decide

# ロゴ画像の置き場所

このフォルダに **`logo.png`** という名前でロゴ画像を置くと、
ゲーム画面・スタート画面・配布ページ（ランディング）のすべてに自動で表示されます。

```
game/assets/logo.png   ← ここに置くだけ
```

- 推奨: 透過PNG（背景が黒なので白〜銀のロゴが映えます）／横幅 800px 以上
- ファイルが無い場合は、絵文字＋テキストの簡易エンブレムが自動で表示されます（フォールバック）
- 置き換え後はコミット＆プッシュするだけで反映されます

{
  "name": "@dock/mcp",
  "version": "12.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "dock-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/server.ts",
    "test": "vitest run --pool=forks --poolOptions.forks.singleFork"
  },
  "dependencies": {
    "@dock/shared": "*",
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}

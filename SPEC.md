# XBUILD データフロー層 — P1/P2 仕様書

対象: `src/geo.js`（統一ジオメトリ型）/ `src/cook.js`（評価エンジン）
状態: **凍結対象**。この2ファイルの公開APIは、以降のノード実装が全て依存するため、実装着手前に確定させる。
検証: `node --test test/*.test.js` — 102 tests / 102 pass
　　　+ Playwright(SwiftShader) によるブラウザ実機検証（§15）。本書の記述はすべて実行検証済み。

---

## 1. 位置づけ

XBUILD は既に2種類のノードグラフを持っている。本仕様はそのうち**下層**を定義する。

| 層 | 役割 | 実体 | 本仕様の対象 |
|---|---|---|---|
| シーン層 | オブジェクトの親子・変換・可視性 | `xbuild_node_editor.html`（Pascal 系レジストリ、`Record<id,Node>` + `parentId`） | 対象外（変更しない） |
| **データフロー層** | ジオメトリの生成・変形 | 本仕様 | **対象** |

**重要な設計判断**: シーン層は書き換えない。データフロー層は、シーン層に **`geo` という新しい kind を1つ追加する**ことで接続する。その `geo` ノードは内部にデータフローグラフを1本持ち、そのグラフの display ノードの出力を自身のジオメトリとして返す。既存の `dirtyNodes` / システム経路とは干渉しない。

これは Pascal 系アーキテクチャで既に検証済みの `ctxRoot` 分離（`bake:'strip'` 相当）と同じ位置づけであり、非破壊拡張パターンに適合する。

---

## 2. 用語の取り扱い

本層の設計思想は既存の商用3DCGソフトのデータフロー設計を参照しているが、**コード・ドキュメントの一切を流用していない**。参照した概念は独立に再実装している。

製品・UI・公開ドキュメントでは、他社の固有名称をそのまま使用しないこと。内部識別子と外部呼称の対応は以下とする。

| 概念 | 内部識別子 | 製品・UI 表記 |
|---|---|---|
| ジオメトリ演算ノード | `GeoOp` | ジオメトリノード |
| 評価 | `cook` | 評価 / 再計算 |
| 要素ごとスクリプト | `wrangle`（※要改名検討, §10.4） | スニペット |
| 再利用可能な部分グラフ | `XDA` | XBUILD アセット |

参考文献として読むべきは Blender Geometry Nodes の**マニュアル**。設計思想が同系統でオープンに文書化されている。ただし Blender は GPL なので、**C++ ソースは参照しない**こと（マニュアル・仕様文書のみ）。

---

## 3. GeoSet — 統一ジオメトリ型

線を流れるものは常に `GeoSet` 1種類のみ。Curve型・Surface型・Number型と分岐させない。これにより任意のノードが任意のノードへ接続でき、部分グラフの再利用（§P4）が成立する。

```
GeoSet
├── point   AttribSet  頂点 / エージェント / GPS点
├── vertex  AttribSet  プリミティブの各コーナー
├── prim    AttribSet  面 / 建物 / 壁
├── detail  AttribSet  シーン全体に1つ（CRS, ENU原点, 敷地情報）
├── primStart : Int32Array  CSR トポロジ
├── vertPoint : Int32Array  CSR トポロジ
└── groups  { point, vertex, prim } : Map<name, Set<index>>
```

### 3.1 4階層が YELLOW にとって重要な理由

この4階層を採用すると、XBUILD の建物頂点・FLOW·LAB の人流エージェント・xPop の GPS 点が**同一の器に載る**。

| 階層 | XBUILD | FLOW·LAB | xPop / xSTADIUM |
|---|---|---|---|
| detail | CRS / ENU原点 / 敷地 | シミュレーション設定 | 集計期間 |
| prim | 建物 / 壁 / 床スラブ | 経路セグメント / ゾーン | メッシュセル / 座席ブロック |
| point | 頂点 | **エージェント現在位置** | GPS 観測点 |
| vertex | 面のコーナー（UV, マイター角） | — | — |

人流シミュレーションは「点属性を毎フレーム更新する GeoOp」になり、ヒートマップは「点属性のプリミティブへの集約」（`promotePointToPrim`）になる。製品間の接着コードが不要になる。

---

## 4. 属性システム

### 4.1 型

| 型名 | 要素数 | 実体 |
|---|---|---|
| `float` | 1 | Float32Array |
| `int` | 1 | Int32Array |
| `vec2` / `vec3` / `vec4` / `quat` | 2 / 3 / 4 / 4 | Float32Array |
| `mat3` / `mat4` | 9 / 16 | Float32Array |
| `string` | 1 | Int32Array + 文字列テーブル |

`string` は文字列そのものを保持せず、`Attribute.strings` テーブルへのインデックスを Int32Array に格納する。建物ID・用途分類など**同じ値が大量に反復する属性**でメモリと比較コストを抑えるため。

### 4.2 予約属性

| 名前 | 階層 | 型 | 意味 |
|---|---|---|---|
| `P` | point | vec3 | 位置。**常に存在する**（GeoSet 構築時に自動生成） |
| `N` | point / vertex | vec3 | 法線 |
| `Cd` | point / prim | vec3 | 色 |
| `name` | prim | string | プリミティブ識別子 |
| `closed` | prim | int | 閉じた多角形か（`addPrim` が自動設定） |

### 4.3 API

```js
const a = geo.addAttrib('point', 'height', 'float');
a.set(i, 12.5);
a.get(i);                     // 12.5
const buf = a.raw();          // 書き込み用の生 TypedArray（ホットループ用）

geo.promotePointToPrim('height', 'mean');   // 'mean'|'min'|'max'|'sum'|'first'
geo.promotePrimToPoint('name');
```

### 4.4 容量と論理要素数

**論理要素数は `AttribSet.count` が唯一の真実**であり、`Attribute.capacity` は確保済み容量にすぎない。両者は一致しない。

反復処理では必ず `geo.point.count` を使うこと。`Attribute.capacity` を要素数と誤用してはならない（実装中に実際に混入したバグ）。

---

## 5. トポロジ（CSR）

```
primStart : Int32Array(primCount + 1)   各プリミティブの vertex 開始位置
vertPoint : Int32Array(vertCount)       vertex -> point 参照
```

プリミティブ `pi` の頂点は `primStart[pi] .. primStart[pi+1]-1`。各 vertex は `vertPoint[vi]` で point を指す。

vertex 階層を point と分離する理由は、**同じ位置に異なる属性を持たせるため**。壁のマイター処理では隣接する2つの壁が同じ point を共有しつつ、コーナーごとに異なる UV / 法線を持つ必要がある。Pascal 系実装での兄弟依存マイター処理は、この階層に自然に載る。

照会 API:

```js
geo.primPoints(pi);   // [pointIndex, ...]
geo.primVerts(pi);    // [vertexIndex, ...]
geo.vertexPrim(vi);   // 所属プリミティブ（二分探索）
```

---

## 6. Copy-on-Write

キャッシュの正しさのため、**ノードは入力ジオメトリを破壊してはならない**。これを規律ではなく機構で保証する。

- `geo.clone()` は実バッファをコピーせず共有し、両者に `frozen` フラグを立てる
- 書き込み時（`set` / `raw` / `reserve`）に初めて実体を複製する
- トポロジ配列（`primStart` / `vertPoint`）も同じ規則で `_topoShared` により保護される

ノード実装側の契約:

| 呼び出し | 意味 |
|---|---|
| `ctx.in(i)` | 入力の COW クローン。**書き換えてよい**。通常はこちらを使う |
| `ctx.inRaw(i)` | 入力の実体。**読み取り専用**。マージ元など複製が無駄な場合のみ |

`ctx.in(i)` は呼ぶたびに新しいクローンを返す。1つの cook 内で複数回呼ばないこと。

### 6.1 実装中に発見した COW 漏れ

`resize` が `frozen` を解除する際に文字列テーブルを切り離しておらず、クローン先での文字列追加が元ジオメトリに漏れていた。回帰テスト `COW regression: resize must not leak the shared string table` で固定済み。**同種の漏れは新しい属性型を追加するたびに再発しうる**ので、型追加時は必ず対応するCOWテストを書くこと。

---

## 7. Cook 契約（遅延プル評価）

### 7.1 ノード定義

```js
registry.register({
  type:    'transform',
  label:   'Transform',
  inputs:  [{ name: 'geo', required: true }],
  params:  { translate: { type: 'vec3', default: [0, 0, 0] } },
  timeDep: false,
  cook(ctx) { /* -> GeoSet を返すこと */ },
});
```

`ctx` の内容:

| フィールド | 内容 |
|---|---|
| `in(i)` | 入力 i の COW クローン（未接続なら `null`） |
| `inRaw(i)` | 入力 i の実体（読み取り専用） |
| `params` | 解決済みパラメータ |
| `time` / `frame` | 評価時刻 |
| `node` | 自ノード |
| `warn(msg)` | 警告を積む（エラーにはしない） |
| `GeoSet` | コンストラクタ |

### 7.2 評価の向き

**プッシュではなくプル**である点が現行 `dirtyNodes` との最大の違い。

1. `setParam` / `setInput` は `invalidate()` を呼び、**自ノードと全下流のキャッシュを破棄する**（プッシュされるのは無効化フラグのみ）
2. 実際の計算は `cook(displayNode)` が呼ばれたときに、末端から上流へ遡って必要なノードだけ実行される
3. 各ノードは出力 `GeoSet` を `_cache` に保持する

結果として、**表示されていない枝は計算されない**。1,644棟の文脈レイヤを持ちながら編集中の建物だけを再計算できるのはこの性質による。

### 7.3 検証済みの挙動

| テスト | 保証される性質 |
|---|---|
| `cook produces geometry and caches it` | 2回目の cook はキャッシュヒット |
| `setParam invalidates downstream only` | 上流は再計算されない |
| `upstream change invalidates the whole downstream chain` | 上流変更は連鎖する |
| `serialize / deserialize round-trips` | 保存 → 復元 → 再 cook が同一結果 |

---

## 8. 時間依存

`timeDep: true` を宣言したノードと、**その下流すべて**が時間依存とみなされる（`isTimeDependent` が上流を再帰探索する）。

- 時間依存ノード: `ctx.time` が変わると再 cook
- 非時間依存ノード: 時刻が変わってもキャッシュを返す

これが FLOW·LAB の核になる。建物ジオメトリは1回 cook して固定、エージェント位置だけ毎フレーム更新される。ベンチでは grid→transform→oscillate の連鎖で、時刻変更時に再 cook されるのは oscillate 1ノードのみであることを確認済み。

### 8.1 動的な時間依存（P3 で解決済み）

`def.timeDep` は真偽値だけでなく **`(node) => boolean` の関数**でもよい。

```js
timeDep(node) { return snippetIsTimeDependent(node.params.code); }
```

これによりスニペットノードは、ソースが `@Time` / `@Frame` を参照していれば自動的に時間依存になる。ユーザーがフラグを立て忘れて「アニメーションしない」という原因の分かりにくい不具合になる経路を塞いだ。判定は生ソースへの `/@(Time|Frame)\b/` で行うため、`f@Timeless` のような長い識別子には誤反応しない。

---

## 9. エラーとレポート

例外は握りつぶさず、**ノードに載せて下流へ伝播させる**。

- `cook` 内の例外は捕捉され `node.error` に格納、出力は空 GeoSet
- 下流ノードは `upstream error in <id>: ...` を自身の error として持つ
- 必須入力の未接続はスローせず error として報告される
- 循環参照は検出され、スタックオーバーフローしない

`graph.report()` はノードごとに以下を返す:

```js
{ nodeId: { type, ms, points, prims, timeDep, error, warnings } }
```

### 9.1 これが AI エージェントの自己修正ループの土台になる

前回の Nodi3D 系プロトタイプの結論は「評価結果のフィードバックループが欠けている」だった。`report()` はその欠落を埋めるために設計されている。エージェントには次を渡す:

1. `registry.catalog()` — 利用可能なノード型とパラメータ
2. `graph.serialize()` — 現在のグラフ
3. `graph.report()` — 各ノードの出力数・実行時間・エラー・警告

エラー文字列が「どのノードで何が起きたか」を含んでいるため、修正対象の特定が推測ではなく参照になる。

---

## 10. スニペットノード（`snippet`）

要素ごとに JS スニペットを実行する。ノードを10個並べる代わりに1行書ける。

### 10.1 構文

```js
@P.z = @P.x * 2.0;          // 既存属性のバインド（vec は {x,y,z}）
f@w = @ptnum * 0.5;         // 新規 float 属性を宣言して作成
s@tag = "seat";             // 新規 string 属性
v@up = {x:0, y:0, z:1};     // 新規 vec3 属性
```

型接頭辞: `f`=float, `i`=int, `v`=vec3, `2`=vec2, `4`=vec4, `s`=string

組込み（読み取り専用）: `@ptnum` `@primnum` `@elemnum` `@numpt` `@numprim` `@Time` `@Frame`

宣言なしで存在しない属性を参照した場合は**エラーにせず警告**を積む（`ctx.warn`）。スニペットの構文エラーは `node.error` になる。

### 10.2 なぜこれが AI モデリングの本命か

Claude にノードを10個ワイヤリングさせるより、スニペットを1本生成させるほうが成功率が桁で高い。理由は3つ:

- 生成対象が構造化グラフではなくテキスト1本になる
- 失敗が行番号付きの構文エラーとして返る
- 部分的に正しいスニペットを差分修正できる（グラフは部分修正が難しい）

### 10.3 性能

13,152 点に対するスニペット実行は約 27–30ms。数万点規模までは実用範囲。10万点を超える場合は、`raw()` で TypedArray を直接触る専用ノードに置き換えること。

### 10.4 改名（P3 で完了）

型名は `snippet`、ソースを持つパラメータは `code`。旧名 `wrangle` / 旧パラメータ `snippet` は読み込み時に自動で移行される（§16.1）。

---

## 11. 性能実測

PLATEAU 文脈相当（1,644棟 → 13,152 点 / 8,220 プリミティブ / 建物名と高さの prim 属性つき）:

| 項目 | 実測 |
|---|---|
| コールド cook（生成 + スニペット） | 83 ms |
| 生成ノード単体 | 45–48 ms |
| キャッシュヒット | 0 ms |
| スニペットのみ再 cook（上流はキャッシュ） | 27–30 ms |
| ヒープ | 約 5 MB |

### 11.1 実装中に潰した O(n²)

初版は `addPoint` / `addPrim` が呼び出しごとに配列を再確保しており、同条件で **506 ms** かかっていた。容量倍化に変更して **48 ms（10.5倍）** に短縮。

`geo.reserve(nPoints, nVerts, nPrims)` も用意したが、倍化だけで 45ms に到達するため**効果はほぼ無い**（93ms → 83ms、大半は測定ノイズ）。API としては残すが、呼ばなくても性能は出る。

---

## 12. 検証手順

P1/P2 は **DOM も three.js も参照しない純粋 JS**。これが今回の移植で最も価値のある性質で、ブラウザを起動せずにテストが回る。

```bash
node --check src/geo.js src/cook.js src/nodes.js
node --test test/basic.test.js
```

いつもの Playwright + SwiftShader + ピクセル検証が必要になるのは P6（ビューア結合）のみ。P1〜P5 の反復速度はこれで数十倍変わる。

---

## 13. 実装マイルストーン

| | 内容 | 状態 |
|---|---|---|
| **P1** | GeoSet + 属性4階層 + COW | **完了（本仕様）** |
| **P2** | Cook エンジン（遅延プル / キャッシュ / 時間依存 / エラー伝播） | **完了（本仕様）** |
| **P3** | スニペットノード確定（改名 + `@Time` 自動検出 + 属性操作ノード） | **完了（§16）** |
| **P4** | サブネット + パラメータ昇格（XDA） | **完了（§18）** |
| **P5** | For-Each ブロック（1,644棟の棟別処理） | **完了（§19）** |
| **P6** | シーン層への `geo` kind 追加 + three.js 変換 | **完了（§15）** |
| **C** | 実データ結合（PLATEAU 3D Tiles / OSM） | **完了（§17）** |
| **P7** | 時系列チャンネル層（xPop / xAD） | 構想のみ |

### P4 の設計方針（着手前メモ。§18 でこの方針どおり実装した）

サブネットが成立するのは、インターフェースが常に「GeoSet in / GeoSet out」だから。P1 で統一型を確定させたのはこのためであり、順序を入れ替えてはならない。

パラメータ昇格の実装形:
- サブネットは自身のパラメータ定義を持つ
- 内部ノードは `"@parent.size"` のような相対参照でそれを読む
- パラメータ解決は cook 時に行い、解決結果をキャッシュキーに含める

**XDA は配布・課金の単位になりうる。** 「アリーナ座席ボウル」「工場ライン」「PLATEAU 文脈」をバージョン付きパラメトリック資産として納品できる。P4 の設計時点で、バージョン番号と後方互換の扱いを決めておくこと。

---

## 14. 既知の制約

| 制約 | 影響 |
|---|---|
| プリミティブは多角形のみ | NURBS / 曲面 / ボリュームは非対応 |
| `mat3` / `mat4` 属性は型のみ定義、演算ヘルパなし | 変換行列の合成は呼び出し側の責務 |
| グループは `Set<index>` で保持 | 数十万要素では Uint8Array ビットマスクへ要変更 |
| キャッシュは無制限 | 大規模グラフでメモリ上限管理（LRU）が必要になる |
| スニペットは `new Function` | 信頼できない入力を実行しない前提。外部共有時は要サンドボックス |
| 属性の型変換は scalar 中心 | `attribconvert` は float ↔ int と vec 成分抽出のみ。scalar → vec の展開は未実装 |
| 属性の昇格は point ↔ prim のみ | vertex 絡みの昇格は未対応（エラーとして報告される） |


---

## 15. P6 — シーン層結合の検証結果

成果物: `dist/xbuild_dataflow.html`（単一 HTML / three.js r128 インライン / ビルド不要 / 654KB）

### 15.1 検証したかった構造的論点

**vertex 階層（面のコーナー）を three.js の非インデックス頂点へ 1:1 で写せるか。**

写せた。テスト `vertex-layer attributes map 1:1 onto non-indexed render vertices` で、同じ point を共有する床と壁が、コーナーごとに別の法線を保持したまま描画バッファへ落ちることを確認済み。これは壁のマイター処理・面ごとの UV に必要な性質であり、**P1 のトポロジ設計は正しい**と結論できる。

法線の優先順位は vertex 属性 > point 属性 > 面法線（Newell）。

### 15.2 非破壊拡張の契約

シーン層は `GeoSet` を一切知らない。`geo` kind が持つのは以下2つのメソッドだけ。

```js
geo: {
  build(node, ctx)            -> 描画バッファ
  isTimeDependent(node)       -> boolean（データフロー層へ委譲）
}
```

既存の `xbuild_node_editor.html` へは、**このシグネチャの kind を1件追加するだけ**で接続できる。`dirtyNodes` / GeometrySystem の経路には触れない。

### 15.3 三角形分割

多角形は耳切り法（ear clipping）で分割する。凹多角形に対応済み（扇分割では L 字形状で面積が合わない）。任意平面へ射影してから 2D で処理するため、壁面のような非軸平行な面も正しく扱える。

未対応: 自己交差多角形、穴あき多角形（開口部は CSG ではなく分割生成で対処する Pascal 系の方針を踏襲する）。

### 15.4 ブラウザ実機での挙動確認

4レイヤ（文脈建物 / パラメトリック建物 / ヒートマップ / 人流エージェント）を同時表示し、以下を確認した。ページエラー 0件。

| 操作 | 期待 | 実測 |
|---|---|---|
| 時刻を進める | 時間依存の2ノードのみ再計算 | `agents1` `heat1` のみ再計算、他4ノードはキャッシュ |
| `tower1.floors` 9→16 | tower グラフのみ再計算 | `tower1` `tw` のみ再計算、三角形 4,250→4,320 |
| `ctx1.count` 380→520 | city グラフのみ再計算 | `ctx1` `cityw` のみ再計算、三角形 →5,720 |
| スニペット編集 | 当該ノードのみ再計算 | `tw` のみ再計算 |
| スニペット構文エラー | UI にエラー表示、ジオメトリは空 | `wrangle syntax error: Unexpected token ';'` を表示、他レイヤは無傷 |
| 未宣言属性の参照 | エラーではなく警告 | `unbound attribute @nosuch (declare it, e.g. f@nosuch)` |

**遅延プル評価がシーン層越しに機能していることの証明**として、`Graph.cookCount` の差分を直接観測した。時刻を1ステップ進めたとき、city グラフと tower グラフのカウンタは変化せず、flow グラフのみ +2 だった。

### 15.5 キャッシュ判定の実装メモ

「再計算」か「キャッシュ」かは、**フレーム開始時点の `Graph.cookCount` を基準値として記録し、各ノードの `stats.serial` と比較する**ことで判定する。

初版はグラフ横断で `serial` の最大値と比較していたが、これは常に「再計算」を返す誤りだった（グラフごとにカウンタが独立しているため）。同種の誤りは、複数グラフを跨ぐ集計を書くたびに再発しうる。

### 15.6 3製品が同一の器に載ることの実証

デモは以下を1つの `GeoSet` 表現の上で同時に動かしている。

| レイヤ | 使う階層 | 対応製品 |
|---|---|---|
| 文脈建物 520棟 | prim（`name` / `height` / `part`） | XBUILD / PLATEAU |
| パラメトリック建物 | prim（`part` / `level`）+ point | XBUILD |
| 人流エージェント 700点 | point（`speed`）、時間依存 | FLOW·LAB |
| ヒートマップ | point → prim 集約（`density`） | xPop / xSTADIUM |

ヒートマップノードは人流エージェントの出力を受けて点をセルへ集約する。**製品間の接着コードは書いていない**。§3.1 の設計判断が実際に機能することの確認になっている。

### 15.7 ビルド

`build.js` が `src/*.js` を無改造のまま極小の CommonJS ローダで包み、three.js r128 とともに単一 HTML へ結合する。

```bash
node build.js                      # -> dist/xbuild_dataflow.html
node --check dist/_bundle_check.js # HTML ではなく抽出スクリプトに対して実行する
```

モジュール側に `import` / `export` を書かない限り、開発時の分割と納品時の単一ファイルが両立する。

### 15.8 P6 で判明した積み残し

| 項目 | 内容 |
|---|---|
| ピッキング未実装 | `primId` バッファは出力済みだが、レイキャストからプリミティブを引く経路は未接続 |
| ジオメトリ全再構築 | 再計算時に BufferGeometry を作り直している。数万面規模では差分更新が必要 |
| 影なし | 現状ライティングのみ。静的シャドウマップは P6.5 で検討 |
| LOD なし | 文脈建物が数千棟になると距離別の簡略化が要る |


---

## 16. P3 — スニペットノードの確定と属性操作ノード

### 16.1 改名と後方互換

| 旧 | 現行 |
|---|---|
| 型名 `wrangle` | `snippet` |
| パラメータ `snippet` | `code` |

保存済みグラフを壊さないため、レジストリに2つの仕組みを入れた。

- **型エイリアス** — `def.aliases: ['wrangle']`。`Registry.get` / `resolveType` が解決する。`Node.type` には**現行名が格納される**ので、一度読み込んで保存し直せば旧名は消える。
- **パラメータ移行フック** — `def.migrateParams(params)`。既定値の適用前に呼ばれる。

旧名で保存されたグラフが `deserialize` → `cook` → `serialize` を通り、現行名で出力されることをテストで固定してある。

この仕組みは今後の改名すべてに使える。**破壊的な名前変更を入れるときは、必ず alias と migrateParams を同時に足すこと。**

### 16.2 時間依存の自動検出

§8.1 を参照。あわせて**コンパイルキャッシュ**（ソース文字列 → コンパイル済み関数、上限200件）を入れた。同じスニペットを毎フレーム再コンパイルしない。失敗も記憶するので、壊れたスニペットのまま再生し続けても `new Function` を毎フレーム叩かない。

### 16.3 属性操作ノード

| 型 | 役割 |
|---|---|
| `attribpromote` | 階層間の移動。point ↔ prim。集約は mean / min / max / sum / first |
| `attribconvert` | float ↔ int の変換、および vec からの成分抽出（`component`） |
| `attribrename` | 改名 |
| `attribdelete` | 削除（空白 / カンマ区切りで複数可） |

いずれも不正な指定は**例外ではなくノードのエラー / 警告**として報告される。未対応の昇格方向（point → vertex 等）は「未対応」と明示され、黙って通らない。

### 16.4 型の取り違えに対する2段構えの防御

同じ名前の属性を別の型で取り直すのは、実際に起きるバグ。対処を2段に分けた。

- **`AttribSet.add` は型が食い違えば例外を投げる** — 内部 API の誤用を早期に落とす
- **スニペットの型宣言が既存と食い違う場合は警告にとどめ、既存の型を優先する** — ユーザーの入力でグラフ全体が止まらないようにする

前者を例外、後者を警告に分けたのは、**内部 API の誤りは実装バグでありユーザー入力の誤りは日常だから**。両方を例外にすると、スニペットに `i@h` と打った瞬間に下流の全レイヤが消える。

### 16.5 ブラウザ実機での確認

`dist/xbuild_dataflow.html` にて、ページエラー 0件。

| 操作 | 結果 |
|---|---|
| 建物のスニペットに `@Time` を追加 | アウトライナのタグが `static` → `time` に変化 |
| 同状態で時刻を進める | `tw` `tzh` のみ再計算、上流の `tower1` はキャッシュのまま |
| 頂点座標の実測 | t=0 と t=0.9 で位置が変化することを確認 |
| `@Time` を削除 | タグが `time` → `static` に戻る |

**上流が非時間依存のまま下流だけが時間依存になる**という構成が正しく動くことがこれで確認できた。建物形状の生成コストを払わずに、スニペットによるアニメーションだけを毎フレーム回せる。

デモの建物レイヤには `attribconvert` を組み込んであり、`P` の Z 成分を `zh` という float 属性へ抽出して着色に使っている。派生属性がスニペットを書かずに作れることの実例。


---

## 17. 実データ結合 — 東静岡アリーナ予定地

成果物: `dist/xbuild_dataflow.html`（単一 HTML / 768KB / ビルド不要）
対象地: 静岡市葵区東静岡一丁目 29〜33 および 37、JR東静岡駅北口市有地

### 17.1 投入したデータ

| 種別 | 出所 | 規模 |
|---|---|---|
| 文脈建物 | PLATEAU 静岡市 建築物モデル LOD1 (2023)、葵区 22101 + 駿河区 22102 | 1,088棟 / 13,582点 / 7,879面 |
| 道路網 | OpenStreetMap（歩行可能な経路のみ） | 374 way / 1,499点 |
| ランドマーク | OpenStreetMap | 5件（東静岡駅・長沼駅・グランシップ・バンダイホビーセンター・東静岡駅北口公園） |
| 人流 | 上記道路網グラフ上の経路歩行 | 900エージェント |
| アリーナ | 静岡市公表の計画値 | 152×111m / 高さ30.9m / 4階 |

### 17.2 抽出パイプライン

```
datacatalog API
  -> tileset.json（外側）-> tileset.json（内側、1,418 content ノード）
  -> 対象 bbox と交差するタイルのみ取得（44タイル / 41MB）
  -> b3dm 分解（featureTable / batchTable / GLB）
  -> KHR_draco_mesh_compression 展開（POSITION / NORMAL / _BATCHID）
  -> 頂点溶接（§17.3-1）
  -> Y-up -> Z-up 変換 (x, y, z) -> (x, -z, y)
  -> CESIUM_RTC.center 加算で ECEF 復元
  -> ECEF -> ENU（原点 34.9858N, 138.4125E）
  -> _BATCHID で建物ごとに分割
  -> 屋根面（上端 0.6m 以内）の境界エッジを連結して外周リング抽出
  -> 共線頂点の除去
  -> batchTable から属性付与（gml_id / measuredHeight / usage / storeys / year）
  -> デシメートル整数 + 差分符号化で bake（92KB）
```

**バケット後のサイズは 92KB。** 元データ 41MB に対して約 1/450。単一 HTML 納品の制約を守りつつ実データを載せられる。

### 17.3 実データで初めて表面化した不具合（5件）

合成データでは一切出なかった。**この5件を先に潰せたことが、C を P4 より先に実施した最大の成果である。**

#### 1. Draco 出力の頂点重複

36,432頂点 / 12,144面 = ちょうど 3.0 倍。**隣接三角形が頂点インデックスを共有していない。**

このため「境界エッジは1回だけ現れる」という判定が全エッジで成立し、抽出されるフットプリントが例外なく3頂点になっていた（`ring len avg 3.0`）。位置を 1mm で丸めた溶接を挟んで解決。溶接後のリング長分布は 4:948件 / 6:453件 / 8:190件 と、日本の建物として妥当な形になった。

**教訓: 外部から来たメッシュは、トポロジ演算の前に必ず溶接する。**

#### 2. 座標の思い込み（最も危険だった）

東静岡駅の位置を 34.9772 と想定していたが、OSM の実測値は **34.98423**。約 750m のずれ。最初の試行では 35.0125 を使っており、そちらは約 4km ずれていた。

気づいたきっかけは幾何ではなく**内容の不自然さ**だった。抽出結果が「最高 17.6m の低層住宅のみ」となり、駅前としてあり得ない構成になっていた。

**教訓: 地理データの原点は、必ず独立ソース（OSM 等）で検算する。** 変換式が正しくても原点が違えば全部が静かに間違う。ECEF→ENU の実装自体は正しく、デコード結果の緯度経度がタイル宣言 region と一致することで別途確認済み。

#### 3. measuredHeight と幾何形状の不一致

|幾何高さ − measuredHeight| は 中央値 0.90m / p90 2.80m / **最大 24.5m**。

原因は精度ではなく構造だった。**1つの物理構造物が複数の CityGML フィーチャに分割され、すべてが同じ measuredHeight を持つ。** 実例として、170.5m の構造物が 160.3m / 87.5m / 81.5m / 81.4m の4フィーチャに分かれ、4件とも `measuredHeight = 170.5` を持っていた。

`measuredHeight` で押し出すと、この構造物は 170.5m の板が4枚重なった形になる。`sitectx` の `useMh` パラメータで両者を切り替えられるようにし、差分を `mhDelta` prim 属性として出力している。

**教訓: CityGML の属性は「フィーチャ単位」であって「建物単位」ではない。**

#### 4. コメント内の `@Time` を時間依存と誤検出

P3 で入れた自動検出が、説明コメント `// @Time を書くと自動で時間依存になる` に反応し、アリーナレイヤを毎フレーム再計算していた。属性バインドも同様にコメント内の `@height` `@usage` を拾っていた。

`stripNonCode()` でコメント（行・ブロック）と文字列リテラルを除去してから判定・バインドするように修正。文字列は中身を空白に潰し、クォートだけ残す。

**この不具合は P3 のブラウザ検証では出なかった。** 説明コメント付きの現実的なスニペットを UI に置いて初めて出た。**合成的なテストケースは、現実のテキストが持つ雑音を再現しない。**

#### 5. バイナリ batch table と異常値

- 一部タイルの batchTable 列が配列ではなく dict で来る（バイナリ batch table）。リスト前提のコードが `KeyError` で落ちた。型チェックを追加。
- 建築年に `1.0` が 720件混入。1800年以前を無効として除外。
- 85棟に `measuredHeight` が存在しない。警告として報告。

### 17.4 干渉チェックを機能にした

当初アリーナを推測で (20, 40) に配置したところ、既存 9 棟と重なった。位置を勘で調整するのは誤り。

**フットプリント内に重心を持つ既存建物の棟数と延べ面積を計算し、detail 属性 `clashes` / `clashArea` として出力、0 でなければ警告する**方式に変更した。

原点 150m 以内を粗探索した結果、(cx=55, cy=65, rotate=−26°) で干渉 0。これを既定値とした。

なお、干渉 0 になる配置は原点 150m 以内ではこの近傍に限られ、単純な軸平行配置では最小でも 1 棟が残る。**152×111m という建築面積が、現況の街区に対して余裕がないことを数値で示せた**こと自体が成果である。

### 17.5 実データ規模での遅延プル評価

時刻を 1 ステップ進めたときの評価レポート:

| ノード | ms | 点 | 面 | 状態 |
|---|---|---|---|---|
| ctx1 (PLATEAU) | 67 | 13,582 | 7,879 | キャッシュ |
| cityw (スニペット) | 76 | 13,582 | 7,879 | キャッシュ |
| arena1 | 1 | 32 | 21 | キャッシュ |
| roads1 (OSM) | 2 | 1,499 | 351 | キャッシュ |
| lm1 | 0 | 10 | 5 | キャッシュ |
| **agents1** | **1** | **900** | 0 | **再計算** |
| **heat1** | **2** | 1,672 | 418 | **再計算** |

**毎フレーム動くのは 3ms 分だけ。** 13,582点の文脈建物を持ちながら人流アニメーションが回るのは、§7 の遅延プル評価と §8 の時間依存伝播が実データ規模でも機能しているため。

### 17.6 エージェントを時刻の純関数にする設計

cook は任意の時刻で呼ばれ、結果がキャッシュされる。したがって**エージェント位置を逐次更新（前フレームの状態に依存）で実装してはならない**。

採用した設計:

1. 歩行可能な OSM way からグラフを構築（0.5m で頂点溶接）→ メモ化
2. エージェントごとに決定論的な経路（グラフ上のランダムウォーク）と累積弧長を生成 → メモ化
3. 時刻 t での位置は `(offset + t × speed) mod 経路長` の弧長位置を二分探索で求める

これにより、キャッシュが失われて任意の時刻で再 cook されても同じ結果になる。テスト `agent positions are a pure function of time and reproducible` で固定。

`attract` パラメータ（0=ランダム歩行、1=会場へ直行）は経路生成時の分岐選択に効く。テストでは `attract=0.9` の方が `attract=0` より会場までの平均距離が短くなることを確認している。

### 17.7 追加したノード

| 型 | 役割 |
|---|---|
| `sitectx` | PLATEAU LOD1 実建物の押し出し。prim 属性 height / usage / storeys / year / part / mhDelta / bid（棟インデックス。棟別 For-Each のキー、§19） |
| `siteroads` | OSM 道路網（開いたプリミティブ = 線分） |
| `roadagents` | 実道路網上の人流エージェント（時間依存、時刻の純関数） |
| `landmarks` | OSM ランドマークの縦線マーカー |
| `arena` | 計画アリーナのマッシング + 既存建物との干渉チェック |

### 17.8 積み残し

| 項目 | 内容 |
|---|---|
| 人流に需要の裏付けがない | 現状はランダム歩行 + 誘引率。来場者数・駅乗降客数に基づく流入モデルが必要 |
| ペデストリアンデッキ未反映 | 東静岡駅・長沼駅を結ぶ計画デッキが道路網に入っていない。経路分散の比較ができない |
| 地形なし | 標高は建物の base に含まれるのみ。DEM は未導入 |
| LOD2 未使用 | 屋根形状のある LOD2 は葵区・清水区にのみ存在。駿河区は LOD1 のみ |
| 干渉判定は重心基準 | フットプリント同士の多角形交差ではなく、重心が矩形内かで判定している |

---

## 18. P4 — サブネット + パラメータ昇格 + XDA

対象: `src/subnet.js`（ノード定義 / XDA）+ `src/cook.js`（`@parent` 解決と文脈同期）
検証: `test/p4.test.js` 21件 + Playwright(SwiftShader) 実機（ページエラー 0件、
バンドル内レジストリでサブネットを組み立てて昇格値の変更が反映されることを確認）。

### 18.1 構成要素

| 型 | 役割 |
|---|---|
| `subnet` | 内側グラフを1本持つ編集可能なコンテナ。入力4口（`in0`〜`in3`） |
| `subinput` | 外側から渡されたジオメトリを内側へ持ち込む（`index` で口を選ぶ） |
| `suboutput` | 内側グラフの出力マーカー。`displayNode` 未設定時はこれが出力になる |

`subnet` のパラメータは3つ。いずれも serialize にそのまま載る。

```js
{
  graph:  { nodes: [...], displayNode },        // 内側グラフ（Graph.serialize 形式）
  defs:   { size: { type:'float', default:10 } }, // 昇格パラメータ定義
  values: { size: 12 },                          // 昇格パラメータの現在値
}
```

### 18.2 パラメータ昇格

内側ノードのパラメータ値に **`'@parent.name'` という文字列**を書くと、cook 時に
親（サブネット / XDA）の昇格パラメータへ解決される（§13 の設計方針どおり）。

- 参照は完全一致の文字列のみ。式・埋め込み（`'@parent.a + 1'`）は不可
- 配列・オブジェクトの内側でも解決される（`values: { move: '@parent.lift' }` —
  これが入れ子サブネットの委譲になる）
- `'graph'` という名前のキーの中身だけは走査しない（内側グラフの保存データは
  別スコープで解決されるため）
- 解決できない参照は**例外ではなくノードエラー**（どのパラメータが何を参照した
  かをエラー文字列に含む）

### 18.3 キャッシュとの整合 — 実装中に発見した設計上の罠

初版は「解決結果をノード単位のキャッシュキーに含めて、cook 時に比較する」だけの
実装だった。これは**プル評価と両立しない**。出力側ノード（suboutput 等）のキャッ
シュが命中すると評価が上流へ遡らないため、`@parent` を参照するノードのキーが変わ
っていても誰もそれを見に行かず、古い結果が返り続ける。

既存エンジンの不変条件は「**キャッシュが古くなるのは invalidate 経由のみであり、
invalidate は下流へ伝播する**」である。そこで `Graph.cook()` の冒頭で文脈同期
（`_syncContext`）を行い、

1. 各ノードの `@parent` 解決結果キー（解決失敗も `'!'+message` としてキー化）
2. `def.cookKey(node, tc)` の値（subinput が入力ジオメトリの実体を返す）

が前回 cook 時と異なるノードを **push 型で invalidate に変換する**。無効化が下流
へ伝播するので、上の罠は構造的に起きない。ノード単体のキー比較も残してあるが、
これは防御であって伝播の主経路ではない。

検証済みの性質（`test/p4.test.js`）:

| 性質 | テスト |
|---|---|
| 昇格値の変更は **参照するノードだけ** 再 cook（無関係な上流はキャッシュ） | `promoted value change recooks only ...` |
| 昇格値が同じなら内側は全ノードキャッシュ | `... hits the inner cache` |
| 外側上流の変更は subinput 経由で内側へ伝播、実体が同じなら伝播しない | `upstream change flows through ...` |
| 内側エラーはサブネットのノードエラーとして表面化 | `inner errors surface ...` |
| 内側の時間依存はサブネット自身の時間依存になる（境界越えの遅延プル） | `subnet with a time-dependent ...` |
| 入れ子サブネットの `@parent` 委譲チェーン | `nested subnets chain ...` |
| serialize / deserialize 往復 | `... round-trips` |

### 18.4 XDA — XBUILD アセット

XDA は「グラフを定義側に固定したサブネット」。`registerAsset(reg, asset)` で
ノード型として登録され、昇格パラメータがそのままノードパラメータになる。

```js
{
  xda: 1,                       // 形式バージョン
  name: 'arena_bowl',           // ノード型名になる
  version: '1.2.0',             // MAJOR.MINOR.PATCH（必須、形式検査あり）
  label?, inputs?, aliases?,
  params: { size: { type:'float', default:10 } },
  graph:  { nodes: [...], displayNode },
  migrate?(params, fromVersion) // 旧バージョン読み込みフック
}
```

バージョンの規約（§13 の宿題への回答）:

- ノードは生成時のアセットバージョンを `params._xdaVersion` に保存する。
  serialize に含まれるため、**保存グラフにバージョンが焼き付く**
- 旧バージョンで保存されたノードを読み込むと `migrate(params, fromVersion)` が
  呼ばれてから現行バージョンが刻み直される。パラメータの改名・削除は migrate で
  吸収する（§16.1 の `migrateParams` 機構の上に載っている）
- ユーザーパラメータ名の `_` 開頭は予約（登録時に拒否される）

`assetFromSubnet(subnetNode, { name, version })` が「公開」操作。昇格パラメータの
既定値には**作者が現在セットしている値**が採用される（調整済みの状態が配布形になる）。

アセットの各インスタンスは内側グラフを独立に実体化するので、キャッシュも独立
（`asset instances have independent inner caches` で固定）。`registry.catalog()` は
アセット型に `asset: { name, version }` を含めて返すため、AI エージェントは
バージョンを見て利用可否を判断できる。

### 18.5 レポートの入れ子

`graph.report()` はサブネット / XDA ノードに `inner` フィールドを含め、内側の
ノード別レポートを入れ子で返す。エージェントの自己修正ループ（§9.1）が
サブネットの中まで見通せる。

### 18.6 積み残し

| 項目 | 内容 |
|---|---|
| 式は不可 | 参照は値の差し替えのみ（`[0, '@parent.h', 0]` のような成分単位も可）。`'@parent.a * 2'` のような式は書けない。式が要るときは内側にスニペットを置く |

---

## 19. P5 — For-Each ブロック

対象: `src/foreach.js`（`foreach` ノード + `extractPrims` 分割ユーティリティ）
検証: `test/p5.test.js` 15件 + Playwright(SwiftShader) 実機（ページエラー 0件、
バンドル内で 190棟の棟別処理が 64ms で完走することを確認）。

P4 の内側グラフ機構（`innerGraph` / `subinput` / `suboutput` / 昇格パラメータ）を
そのまま本体グラフとして再利用している。**P4 → P5 の順序はこの依存のため**。

### 19.1 反復モード

| mode | ピースの単位 | 用途 |
|---|---|---|
| `prim` | プリミティブ1つ | 面ごとの処理 |
| `piece` | prim 属性 `attrib` の値が同じプリミティブ群 | **棟別処理**（`sitectx` の `bid` で棟単位） |

ピースの列挙順は属性値の初出順（決定論）。

### 19.2 本体グラフから見える反復情報

同じ情報を2経路で公開する。パラメータには参照で、スニペットには属性で届く。

| 経路 | 名前 | 内容 |
|---|---|---|
| パラメータ参照 | `@parent.iteration` / `@parent.iterations` / `@parent.value` | 反復番号 / 総数 / ピースキー |
| ピースの detail 属性 | `iteration` (int) / `piece`（キー属性と同型） | 同上（`piece` は piece モードのみ） |

foreach 自身も subnet と同じ `defs` / `values` を持ち、昇格パラメータは反復情報と
同じ `@parent` 空間に混ざる。**予約名（iteration / iterations / value）が衝突したら
予約名が勝つ。** 反復メタ情報の detail 属性は結合結果には持ち込まれない
（merge が detail を先勝ちで拾うため、iteration=0 が焼き付くのを防ぐ）。

### 19.3 分割と結合の性質

- ピース内で共有されている point は、ピース内では共有が保たれる（`extractPrims`）
- **ピース間では point は複製される。** prim モードで共有トポロジを流すと
  点数は増える（grid 3×3: 9点 → 4ピース×4点 = 16点）。棟別処理では棟間の
  point 共有が元々無いので点数は変わらない（実測: 13,582点 → 13,582点）
- 属性（point / vertex / prim / detail）はコピーされる。**グループはコピーされない**（既知の制約）
- 本体のエラーは `iteration k (piece "キー")` を先頭に付けて foreach のエラーになる
- 本体の警告はユニーク化して持ち上げ、20件で打ち切って件数を報告する
  （1,000棟が同じ警告を出しても警告欄が爆発しない）

### 19.4 キャッシュ

- foreach ノード自体のキャッシュは通常どおり（入力・パラメータが同じなら 0ms）
- 反復ごとに subinput の実体が替わるため、本体グラフのキャッシュは反復を跨いで
  効かない（`_syncContext` が毎反復 invalidate に変換する）。これは正しい挙動で、
  スニペットのコンパイルキャッシュ（§16.2）が反復間の実質的な共有になる
- `@Time` を含む本体は foreach 全体を時間依存にする（境界越えは P4 と同じ）

### 19.5 性能実測

実データ（PLATEAU 1,088棟 / 7,879面）の棟別 For-Each（本体: スニペット1本）:

| 項目 | 実測 |
|---|---|
| 1,088棟 反復合計 | 142 ms（1棟 ≈ 0.13ms） |
| 内訳の主要因 | ピース分割 + 本体グラフの文脈同期 + merge |

数千棟オーダーまでは実用範囲。反復あたりのコストが固定でかかるため、
数万ピースを回す場合は本体を TypedArray 直接操作の専用ノードにするか、
ピースを粗くする（usage 単位など）こと。

### 19.6 レポート

`report()` の foreach ノードには `inner: { iterations, body }` が載る。
`body` は最終反復時点の本体グラフのレポート。

### 19.7 積み残し

| 項目 | 内容 |
|---|---|
| フィードバック反復なし | 前反復の出力を次反復の入力にするモード（累積型）は未実装。用途が出たときに `feedback` モードとして足す |
| グループ非保存 | `extractPrims` はグループをコピーしない。merge 側は対応済みなので、必要になったら分割側だけ足す |
| 並列化なし | 反復は逐次実行。ピース間は独立なので、必要になれば Worker 分散が可能な構造にはなっている |
| UI 未接続 | サブネットの編集 UI（中に入る / 昇格操作）は P6.5 以降。エンジンと形式だけ先に確定した |
| XDA の配布形式 | 現状は JS オブジェクト。ファイル形式（.xda.json 等）と署名 / 依存解決は配布設計時に決める |
